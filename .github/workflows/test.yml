name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio aioresponses aiohttp ruff mypy
          pip install pytest-homeassistant-custom-component
          pip install "asyncio-mqtt>=0.16"

      - name: Verify add-on API client is in sync
        run: ./scripts/check_sync.sh

      - name: Lint with ruff
        run: ruff check custom_components/ addon/plenitude2mqtt/service/ tests/

      - name: Type check with mypy
        run: mypy custom_components/ addon/plenitude2mqtt/service/

      - name: Run tests
        run: pytest -v
